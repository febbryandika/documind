// TODO: SPEC §9 — ingest fixtures, run golden.json through retrieve(), report
// hit-rate@5, MRR and mean top-hit similarity. Costs OpenAI calls; on demand only.
console.log(
  "TODO: retrieval eval — hit-rate@5 over eval/golden.json — SPEC §9",
);
process.exit(0);
