{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:generate": "echo 'TODO: bun drizzle-kit generate — SPEC §4, build order step 1'",
    "db:migrate": "echo 'TODO: bun drizzle-kit migrate — SPEC §4, build order step 1'",
    "db:seed": "bun run src/db/seed.ts",
    "eval": "bun run eval/run.ts"
  },
  "dependencies": {
    "hono": "^4.13.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/bun": "^1.4.0",
    "eslint": "^10.9.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.67.0",
    "vitest": "^4.1.11"
  }
}
